# Stride Brain Benchmark Reference

This directory contains tracked copies of the Stride forward and inverse brain
benchmark scripts used by the repository's benchmark wrapper.

They are intentionally kept close to the original local reference scripts, but
their model paths are resolved relative to the repository root so a fresh clone
can run them without depending on an untracked `resources/` directory.
