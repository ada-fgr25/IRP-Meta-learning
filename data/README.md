# Data

This directory contains tracked input data for experiments.

Current contents:

- `alpha2D-TrueModel.h5`: Stride reference brain velocity model
- `alpha2D-StartingModel.h5`: Stride reference starting model

These files are copied from the local Stride example resources so the baseline
can use visible, versioned data rather than relying on the gitignored
`resources/` directory.
